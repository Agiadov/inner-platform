'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
export default function Profile(){const [p,setP]=useState<any>({});const [ok,setOk]=useState(false);useEffect(()=>setP(JSON.parse(localStorage.getItem('inner_user_profile')||'{}')),[]);const set=(k:string,v:string)=>setP((x:any)=>({...x,[k]:v}));const save=()=>{localStorage.setItem('inner_user_profile',JSON.stringify(p));setOk(true)};return <main className="container"><section className="card"><h1>Профиль</h1>{ok&&<p className="notice">Профиль сохранён</p>}<div className="grid grid3"><div className="field"><label>Имя</label><input value={p.name||''} onChange={e=>set('name',e.target.value)}/></div><div className="field"><label>Telegram</label><input value={p.telegram||''} onChange={e=>set('telegram',e.target.value)}/></div><div className="field"><label>Телефон / WhatsApp</label><input value={p.phone||''} onChange={e=>set('phone',e.target.value)}/></div><div className="field"><label>Email</label><input value={p.email||''} onChange={e=>set('email',e.target.value)}/></div><div className="field"><label>Город</label><input value={p.city||''} onChange={e=>set('city',e.target.value)}/></div><div className="field"><label>Адрес доставки</label><input value={p.address||''} onChange={e=>set('address',e.target.value)}/></div><div className="field"><label>Размер одежды</label><input value={p.clothingSize||''} onChange={e=>set('clothingSize',e.target.value)}/></div><div className="field"><label>Размер обуви</label><input value={p.shoeSize||''} onChange={e=>set('shoeSize',e.target.value)}/></div><div className="field"><label>Любимые бренды</label><input value={p.brands||''} onChange={e=>set('brands',e.target.value)}/></div></div><div className="row"><button className="btn" onClick={save}>Сохранить</button><Link className="btn secondary" href="/request">Создать заявку с этими данными</Link></div></section></main>}
